apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: "82263412-ingress"
spec:
  ingressClassName: nginx
  rules:
  - host: "82263412-prod.cepg-aa.kubepia.net"
    http:
      paths:
      - path: /
        backend:
          service:
            name: my-82263412
            port:
              number: 8080
        pathType: Prefix
